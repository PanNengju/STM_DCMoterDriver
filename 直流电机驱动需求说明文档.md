# 基于STM32的直流电机驱动控制系统 — 需求规格说明书

## 文档信息

| 项目 | 内容 |
|------|------|
| 文档名称 | 直流电机驱动控制系统 需求规格说明书 |
| 主控平台 | STM32F4 系列（兼容 F1/H7） |
| 测试电机 | 东政带编码器直流电机 |
| 文档版本 | V1.1 |
| 编制日期 | 2026-05-23 |
| 修订记录 | V1.1 — 通信协议切换为 CANopen（CiA 301/402），UART 改为串口调试 |

---

## 1. 系统概述

### 1.1 项目背景

设计一套基于 STM32 单片机的直流电机驱动控制系统，实现速度、位置、力矩三种核心控制模式，并具备完善的保护与报警机制。系统需要兼容多种直流电机，当前以**东政带编码器电机**作为测试验证平台。

### 1.2 系统目标

- 实现直流电机的**三闭环控制**（电流环、速度环、位置环）
- 提供**实时状态反馈**（速度、位置、电流、扭矩）
- 建立**多级保护机制**（过载、过流、堵转、过热）
- 支持**紧急停止**与**故障复位**
- 具备**传感器归零**功能
- 兼容多种电机型号，通过参数配置适配

---

## 2. 系统架构

### 2.1 硬件架构

```
┌─────────────────────────────────────────────────────┐
│                    STM32 主控                        │
│  ┌──────────┐  ┌──────────┐  ┌──────────────────┐   │
│  │  PWM 输出 │  │ 编码器接口│  │  ADC 采样        │   │
│  │  (TIM1)  │  │  (TIM2/3)│  │  (电流/电压/温度) │   │
│  └────┬─────┘  └────┬─────┘  └───────┬──────────┘   │
│       │             │               │               │
│  ┌────▼─────┐  ┌────▼─────┐  ┌──────▼───────────┐   │
│  │ H桥驱动  │  │ 编码器    │  │ 霍尔/电流传感器   │   │
│  │ (MOSFET) │  │ (AB相)   │  │ (ACS712/INA226)  │   │
│  └────┬─────┘  └────┬─────┘  └──────┬───────────┘   │
│       │             │               │               │
│  ┌────▼─────────────▼───────────────▼───────────┐    │
│  │              直流电机（带编码器）              │    │
│  └──────────────────────────────────────────────┘    │
│                                                     │
│  ┌──────────┐  ┌──────────┐  ┌──────────────────┐   │
│  │ 急停按钮 │  │ 温度传感器│  │ CANopen(CAN)/UART调试│   │
│  └──────────┘  └──────────┘  └──────────────────┘   │
└─────────────────────────────────────────────────────┘
```

### 2.2 软件架构

采用**分层模块化**设计：

| 层级 | 模块 | 职责 |
|------|------|------|
| 应用层 | CANopen 协议栈、控制指令解析、运行状态机 | 接收指令、调度控制模式 |
| 控制层 | PID 控制器（电流/速度/位置） | 三环闭环计算 |
| 驱动层 | PWM 输出、编码器读取、ADC 采集 | 硬件抽象 |
| 系统层 | FreeRTOS 任务调度、看门狗 | 实时调度与系统保护 |

### 2.3 控制回路

```
位置给定 → 位置环PID → 速度环PID → 电流环PID → PWM占空比 → 电机
               ↑            ↑            ↑
           编码器位置    编码器速度    ADC电流采样
```

- **电流环**：控制周期 50μs（20kHz），最内环
- **速度环**：控制周期 1ms（1kHz）
- **位置环**：控制周期 5ms（200Hz）

---

## 3. 功能需求

### 3.1 速度控制模式

| 需求编号 | FR-SP-001 |
|----------|-----------|
| 描述 | 控制电机以指定速度运行，支持正反转 |
| 输入 | 目标速度（RPM），范围可配置，默认 -3000 ~ +3000 RPM |
| 输出 | PWM 驱动信号 |
| 控制算法 | 速度环 PID + 电流环 PID 串级控制 |
| 性能 | 稳态误差 ≤ ±1%，超调 ≤ 5%，调节时间 ≤ 200ms |

#### 子功能

- FR-SP-001-01：支持**梯形速度曲线**——设定加速度/减速度，实现平滑启停
- FR-SP-001-02：支持**S形速度曲线**——设定加加速度（jerk），进一步降低冲击
- FR-SP-001-03：支持**速度前馈**——根据目标速度计算前馈占空比，提升响应速度

### 3.2 位置控制模式

| 需求编号 | FR-POS-001 |
|----------|------------|
| 描述 | 控制电机旋转到指定位置（绝对/相对） |
| 输入 | 目标位置（脉冲数/角度），相对/绝对模式选择 |
| 输出 | PWM 驱动信号 |
| 控制算法 | 位置环 PID + 速度环 PID + 电流环 PID 三环串级 |
| 性能 | 定位精度 ≤ ±2个编码器脉冲，无超调定位 |

#### 子功能

- FR-POS-001-01：支持**绝对位置模式**——以归零后的零点为参考
- FR-POS-001-02：支持**相对位置模式**——以当前位置为参考，走指定增量
- FR-POS-001-03：支持**多段位置序列**——预设多组位置，顺序执行
- FR-POS-001-04：支持**位置限位**——软件限位（最大/最小位置），可选硬件限位开关

### 3.3 力矩/扭矩控制模式

| 需求编号 | FR-TRQ-001 |
|----------|------------|
| 描述 | 控制电机输出指定扭矩（电流） |
| 输入 | 目标扭矩（Nm）或目标电流（A） |
| 输出 | PWM 驱动信号 |
| 控制算法 | 电流环 PID 单环控制 |
| 性能 | 电流稳态误差 ≤ ±2%，响应时间 ≤ 10ms |

#### 子功能

- FR-TRQ-001-01：支持**扭矩限幅**——在速度/位置模式下作为内环限制最大扭矩输出
- FR-TRQ-001-02：支持**扭矩斜坡**——设定扭矩上升/下降速率

### 3.4 紧急停止

| 需求编号 | FR-ESTOP-001 |
|----------|--------------|
| 描述 | 通过硬件按钮或软件指令立即停止电机 |
| 触发方式 | 硬件急停按钮（外部中断，最高优先级）<br>软件急停指令（通信接口）<br>故障触发自动急停 |
| 响应时间 | ≤ 5ms（从触发到 PWM 输出关闭） |
| 停止方式 | PWM 立即关闭（惯性停止）/ 主动制动（下桥臂导通）可配置 |

#### 子功能

- FR-ESTOP-001-01：急停后进入**锁定状态**，需手动复位才能重新启动
- FR-ESTOP-001-02：急停状态通过**LED 指示灯**和**状态字**反馈

### 3.5 错误复位

| 需求编号 | FR-RESET-001 |
|----------|--------------|
| 描述 | 在故障排除后，通过软件指令复位错误状态 |
| 输入 | 复位指令（通信接口） |
| 处理流程 | 1. 检查故障源是否消除<br>2. 清除错误标志位<br>3. 恢复控制器初始状态<br>4. 返回就绪状态 |
| 约束 | 若故障源未消除，复位后立即再次触发保护，拒绝进入就绪状态 |

### 3.6 传感器归零

| 需求编号 | FR-ZERO-001 |
|----------|-------------|
| 描述 | 将编码器当前位置设定为零点参考 |
| 触发方式 | 软件指令 / 外部归零传感器（限位开关或光电传感器） |
| 处理流程 | 1. 记录当前编码器计数值作为零点偏移<br>2. 后续位置反馈 = 编码器原始值 - 零点偏移<br>3. 零点数据写入 Flash/EEPROM 持久保存 |

#### 子功能

- FR-ZERO-001-01：支持**自动归零**——电机低速运行至限位开关触发，自动设定零点
- FR-ZERO-001-02：支持**手动归零**——操作员手动对位后发送归零指令
- FR-ZERO-001-03：零点掉电保存，上电自动恢复

---

## 4. 实时反馈需求

### 4.1 反馈数据列表

| 需求编号 | 反馈量 | 数据来源 | 更新频率 | 精度 | 单位 |
|----------|--------|----------|----------|------|------|
| FR-FB-001 | 实时速度 | 编码器测速（M/T法） | 1kHz | ±1 RPM | RPM |
| FR-FB-002 | 当前位置 | 编码器累计计数 | 1kHz | ±1 脉冲 | 脉冲/度 |
| FR-FB-003 | 当前电流 | ADC 采样/电流传感器 | 20kHz | ±5mA | A/mA |
| FR-FB-004 | 当前扭矩 | 电流 × 扭矩常数换算 | 1kHz | ±0.01 Nm | Nm |
| FR-FB-005 | 母线电压 | ADC 采样 | 100Hz | ±0.1V | V |
| FR-FB-006 | 驱动器温度 | 温度传感器（NTC/DS18B20） | 10Hz | ±1℃ | ℃ |
| FR-FB-007 | 电机温度 | 温度传感器（可选，NTC） | 10Hz | ±1℃ | ℃ |
| FR-FB-008 | 运行状态 | 状态机标志位 | 事件驱动 | — | 枚举 |

### 4.2 反馈数据格式（通信协议）

反馈数据帧（20字节）：

| 字节偏移 | 字段 | 类型 | 说明 |
|----------|------|------|------|
| 0-1 | 帧头 | u16 | 0xA55A |
| 2 | 状态字 | u8 | bit0:使能 bit1:运行 bit2:故障 bit3:急停 bit4-7:保留 |
| 3 | 错误码 | u8 | 0=正常, 其他见错误码表 |
| 4-7 | 速度 | s32 | 实际速度，单位 RPM×100 |
| 8-11 | 位置 | s32 | 当前位置，单位脉冲 |
| 12-15 | 电流 | s32 | 实际电流，单位 mA |
| 16-17 | 扭矩 | s16 | 实际扭矩，单位 Nm×100 |
| 18 | 温度 | u8 | 驱动器温度，偏移40℃ |
| 19 | 校验 | u8 | CRC-8 / 和校验 |

---

## 5. 报警与保护需求

### 5.1 报警类型

| 需求编号 | 报警类型 | 触发条件 | 阈值可配置 | 处理方式 | 恢复方式 |
|----------|----------|----------|------------|----------|----------|
| FR-ALM-001 | 过载报警 | 扭矩 > 额定扭矩×150% 持续 > 阈值时间 | 是 | 减速/停机 | 自动恢复 |
| FR-ALM-002 | 过流报警 | 电流 > 额定电流×200% | 是 | 立即停机 | 手动复位 |
| FR-ALM-003 | 堵转报警 | 速度=0 且 电流>堵转判定电流 持续 > 阈值时间 | 是 | 立即停机 | 手动复位 |
| FR-ALM-004 | 过热报警 | 驱动器温度 > 85℃ 或 电机温度 > 120℃ | 是 | 降功率/停机 | 自动恢复(<70℃) |
| FR-ALM-005 | 过压报警 | 母线电压 > 额定电压×120% | 是 | 立即停机 | 手动复位 |
| FR-ALM-006 | 欠压报警 | 母线电压 < 额定电压×70% | 是 | 立即停机 | 自动恢复 |
| FR-ALM-007 | 编码器故障 | 编码器信号异常（AB相同时跳变等） | — | 立即停机 | 手动复位 |
| FR-ALM-008 | 通信超时 | 连续 N ms 未收到有效通信帧 | 是 | 按配置停机/保持 | 自动恢复 |

### 5.2 报警分级

| 等级 | 名称 | 行为 | LED 指示 |
|------|------|------|----------|
| 0 | 正常 | 正常运行 | 绿灯常亮 |
| 1 | 警告 | 继续运行，降功率/限速 | 黄灯闪烁 |
| 2 | 可恢复故障 | 停止输出，可自动恢复 | 红灯闪烁（1Hz） |
| 3 | 严重故障 | 立即停机锁存，需手动复位 | 红灯常亮 |

### 5.3 报警信息上报

- 报警发生时通过 **CANopen EMCY 报文主动上送**（事件驱动），无需上位机轮询
- EMCY 报文包含：标准错误码 + 制造商特定域（触发时刻的速度/电流/温度快照）
- 故障消除时发送 EMCY 复位报文（错误码=0x0000），同时状态字清除故障位
- 严重故障（等级3）需额外的 Fault Reset 指令（Controlword 0x80）才能清除
- 报警历史记录可通过 SDO 读取（Index 0x2050，最近 10 条）

---

## 6. 通信接口需求

### 6.1 通信架构

系统采用**双通道通信**设计：

| 通道 | 物理接口 | 协议 | 用途 | 默认参数 |
|------|----------|------|------|----------|
| 主通信 | CAN | **CANopen**（CiA 301 + CiA 402） | 多电机组网、实时控制、状态反馈 | 1Mbps |
| 调试通道 | UART | 命令行协议（CLI） | 本地调试、参数调优、日志输出 | 115200, 8N1 |

- **CANopen 是系统核心通信协议**，所有的运动控制、状态反馈、故障上报均通过 CANopen 完成
- **UART 用作调试终端**，提供交互式命令行，仅在开发/调试场景使用

### 6.2 CANopen 协议栈实现

#### 6.2.1 协议栈架构

严格遵循 **CiA 301**（应用层与通信行规）和 **CiA 402**（驱动与运动控制设备行规）。

```
┌─────────────────────────────────────────┐
│            CiA 402 驱动行规              │
│  (PDS状态机 / 控制模式 / 反馈)          │
├─────────────────────────────────────────┤
│            CiA 301 应用层                │
│  NMT / SDO / PDO / EMCY / SYNC / HB     │
├─────────────────────────────────────────┤
│            CAN 驱动层                    │
│  STM32 bxCAN (FIFO / 过滤器)            │
└─────────────────────────────────────────┘
```

#### 6.2.2 NMT（网络管理）

| 服务 | 说明 | COB-ID |
|------|------|--------|
| NMT 状态控制 | 主机控制从机状态切换：初始化→预操作→操作→停止 | 0x000 |
| NMT 节点守护 | Heartbeat 心跳机制，周期可配（默认 200ms） | 0x700+NodeID |
| NMT 错误控制 | Heartbeat Consumer 监测，超时判定通信丢失 | — |

**NMT 状态流转**：

```
上电 ──→ 初始化 ──→ 预操作 ──→ 操作状态
                  (SDO可通)  (SDO+PDO通)
                      ↑           │
                      └─── 停止 ←─┘
```

**NMT 控制指令**（COB-ID: 0x000，2字节数据）：

| 指令码 | 名称 | 说明 |
|--------|------|------|
| 0x01 | Start Remote Node | 进入操作状态 |
| 0x02 | Stop Remote Node | 进入停止状态 |
| 0x80 | Enter Pre-Operational | 进入预操作状态 |
| 0x81 | Reset Node | 复位节点（重载参数） |
| 0x82 | Reset Communication | 复位通信层 |

#### 6.2.3 SDO（服务数据对象）

用于**参数配置**、**对象字典读写**、**固件升级**。

| 属性 | 说明 |
|------|------|
| 协议 | SDO 加速传输（≤4字节）+ 分段传输（>4字节） |
| COB-ID | 请求 0x600+NodeID / 应答 0x580+NodeID |
| 速率 | 与 CAN 总线速率一致 |
| 用途 | 电机参数配置、PID 参数写入、保护阈值修改、对象字典访问 |

#### 6.2.4 PDO（过程数据对象）

用于**实时控制与反馈**，映射为映射可变 PDO（即插即用）。

**RPDO（接收 PDO）— 控制指令**（COB-ID: 0x200+NodeID）：

| RPDO 编号 | 映射内容 | 传输方式 | 触发方式 |
|-----------|----------|----------|----------|
| RPDO1 | 控制字(16bit) + 目标值1(32bit) | 同步/异步 | SYNC / 事件 |
| RPDO2 | 控制模式(8bit) + 目标值2(32bit) | 同步/异步 | SYNC / 事件 |
| RPDO3 | 保留（扩展用） | — | — |
| RPDO4 | 保留（扩展用） | — | — |

**TPDO（发送 PDO）— 状态反馈**（COB-ID: 0x180+NodeID）：

| TPDO 编号 | 映射内容 | 传输方式 |
|-----------|----------|----------|
| TPDO1 | 状态字(16bit) + 实际速度(32bit) | 事件 + 定时 |
| TPDO2 | 实际位置(32bit) + 实际电流(32bit) | 事件 + 定时 |
| TPDO3 | 实际扭矩(16bit) + 驱动器温度(8bit) + 母线电压(16bit) | 事件 + 定时 |
| TPDO4 | 保留（电机温度/运行状态等扩展） | — |

> **PDO 传输触发方式**：
> - **同步**：收到 SYNC 帧后发送（位置/速度环同步控制）
> - **异步⋅事件驱动**：状态变化时立即发送（急停、故障等）
> - **异步⋅定时**：周期性发送实时反馈（默认 TPDO1/2 每 10ms 发送）

#### 6.2.5 EMCY（紧急报文）

用于**故障告警与报警信息上报**。

| 属性 | 说明 |
|------|------|
| COB-ID | 0x080+NodeID |
| 8 字节数据 | 错误码(2B) + 错误寄存器(1B) + 制造商特定域(5B) |

**CANopen EMCY 错误码映射**：

| 错误码 | 报警类型 | 制造商特定域（5字节） |
|--------|----------|------------------------|
| 0x2310 | 过流/短路 | [速度(2B) + 电流(2B) + 保留(1B)] |
| 0x3210 | 过压 | [母线电压(2B) + 保留(3B)] |
| 0x3220 | 欠压 | [母线电压(2B) + 保留(3B)] |
| 0x4310 | 驱动器过热 | [驱动温度(1B) + 电机温度(1B) + 保留(3B)] |
| 0x8000 | 过载 | [扭矩(2B) + 持续时长(2B) + 保留(1B)] |
| 0x8611 | 堵转/跟随误差 | [速度(2B) + 电流(2B) + 保留(1B)] |
| 0x7305 | 编码器故障 | [编码器状态(1B) + 保留(4B)] |
| 0x8100 | 通信超时 | [距上次通信ms数(2B) + 保留(3B)] |

**EMCY 报文行为**：
- 故障发生时**立即主动上送** EMCY 报文（事件驱动）
- 故障消除时发送 **EMCY 复位报文**（错误码=0x0000）
- EMCY 报文发送后，对应的 TPDO 在下一个周期同步上报完整数据快照

#### 6.2.6 SYNC（同步对象）

| 属性 | 说明 |
|------|------|
| COB-ID | 0x080 |
| 周期 | 1ms（可配，与速度环控制周期同步） |
| 用途 | 同步多电机控制周期；触发同步类 PDO 发送 |

#### 6.2.7 Heartbeat（心跳报文）

| 属性 | 说明 |
|------|------|
| COB-ID | 0x700+NodeID |
| 周期 | 200ms（可配，范围 50ms ~ 5000ms） |
| 数据 | 1字节：当前 NMT 状态 |
| 消费者 | 上位机监测心跳，超时 3× 周期未收到则判定通信丢失 |

### 6.3 CiA 402 驱动行规映射

#### 6.3.1 PDS（Power Drive System）状态机

根据 CiA 402 规范实现完整的 Power Drive System 状态机：

```
     ┌─── 上电 ──→ Not Ready ──→ Switch On Disabled ──→ Ready ──→ Switched On ──→ Operation
     │                  ↑               │                  ↑             │              │
     │                  │  Quick Stop   │       Shutdown   │             │  Disable     │
     │                  │    ↓          │          ↓       │             │  Voltage     │
     │                  │  Fault ←─── (来自任意状态)       │             ↓              │
     │                  │    │                             │       Quick Stop Active   │
     │                  │    └── Fault Reset ──────────────┘                            │
     └─────────────────────────────────────────────────────────────────────────────────┘
```

**控制字** (Controlword, Index 0x6040)：

| 指令 | Controlword 值 | 说明 |
|------|---------------|------|
| Shutdown | 0x06 | 减速停机，进入 Ready to Switch On |
| Switch On | 0x07 | 上电使能 |
| Enable Operation | 0x0F | 进入运行状态 |
| Quick Stop | 0x02 | 快速停止 |
| Disable Voltage | 0x00 | 关闭 PWM 输出 |
| Fault Reset | 0x80 | 上升沿复位故障 |

**状态字** (Statusword, Index 0x6041) 按标准位定义反馈当前 PDS 状态。

#### 6.3.2 控制模式切换

通过 **Modes of Operation** (Index 0x6060) 切换控制模式：

| Index: 0x6060 值 | 控制模式 | 说明 |
|-------------------|----------|------|
| 1 | Profile Position (PP) | 位置控制模式 |
| 3 | Profile Velocity (PV) | 速度控制模式 |
| 4 | Torque Profile (TQ) | 力矩/扭矩控制模式 |
| 6 | Homing | 归零模式 |

切换模式必须在 **操作状态下** 通过 SDO 写入 0x6060。

**Modes of Operation Display** (Index 0x6061, 只读)：反馈当前实际运行的控制模式。

#### 6.3.3 各控制模式下的对象字典

**速度控制模式** (Profile Velocity, PV)：

| Index | 名称 | 类型 | 说明 |
|-------|------|------|------|
| 0x6060 | Modes of Operation | I8 | 写入 3，切换到速度模式 |
| 0x606B | Velocity Demand | I32 | 写入目标速度 (RPM×100) |
| 0x6042 | vl Target Velocity | I16 | 目标速度 (RPM) |
| 0x606C | Velocity Actual Value | I32 | 实际速度反馈 (RPM×100) |
| 0x6048 | vl Velocity Acceleration | U32 | 加速度 (RPM/s) — 梯形速度曲线 |
| 0x6049 | vl Velocity Deceleration | U32 | 减速度 (RPM/s) |
| 0x60A3 | Profile Jerk | U32 | 加加速度 — S形速度曲线（可选） |

**位置控制模式** (Profile Position, PP)：

| Index | 名称 | 类型 | 说明 |
|-------|------|------|------|
| 0x6060 | Modes of Operation | I8 | 写入 1，切换到位置模式 |
| 0x607A | Target Position | I32 | 目标位置（脉冲数） |
| 0x6064 | Position Actual Value | I32 | 实际位置反馈（脉冲） |
| 0x6081 | Profile Velocity | U32 | 定位过程中的最大速度 |
| 0x6083 | Profile Acceleration | U32 | 定位加速度 |
| 0x6084 | Profile Deceleration | U32 | 定位减速度 |
| 0x60F2 | Positioning Option Code | U16 | 位置选项（绝对/相对等） |
| 0x607D | Software Position Limit Min | I32 | 软件位置下限 |
| 0x607E | Software Position Limit Max | I32 | 软件位置上限 |

**力矩控制模式** (Torque Profile, TQ)：

| Index | 名称 | 类型 | 说明 |
|-------|------|------|------|
| 0x6060 | Modes of Operation | I8 | 写入 4，切换到力矩模式 |
| 0x6071 | Target Torque | I16 | 目标扭矩 (Nm×100) |
| 0x6077 | Torque Actual Value | I16 | 实际扭矩反馈 (Nm×100) |
| 0x6072 | Max Torque | U16 | 最大允许扭矩限幅 |
| 0x6087 | Torque Slope | U32 | 扭矩变化率 (Nm/s) |

**归零模式** (Homing)：

| Index | 名称 | 类型 | 说明 |
|-------|------|------|------|
| 0x6060 | Modes of Operation | I8 | 写入 6，进入归零模式 |
| 0x6098 | Homing Method | I8 | 归零方式：-1=手动 / 17=负极限开关 / 33=正极限开关 |
| 0x6099 | Homing Speed (Switch) | U32 | 寻找限位开关速度 |
| 0x609A | Homing Speed (Zero) | U32 | 归零爬行速度 |
| 0x60E3 | Home Offset | I32 | 零点偏移量 |

### 6.4 对象字典总览（自定义区域）

CANopen 对象字典自定义区域 (Index 0x2000 ~ 0x5FFF) 映射本系统的电机参数与 PID 配置：

| Index 范围 | 内容 | 说明 |
|------------|------|------|
| 0x2000 ~ 0x2010 | 电机参数 | 编码器线数、额定电压/电流、扭矩常数等（详见第7章 ID 对应关系） |
| 0x2020 ~ 0x2030 | PID 参数 | 电流环/速度环/位置环 Kp,Ki,Kd |
| 0x2040 ~ 0x2050 | 保护阈值 | 过载/过流/堵转/过热/过欠压等阈值 |
| 0x2100 ~ 0x2110 | 校准数据 | 零点偏移、编码器校准值、电流零漂补偿 |
| 0x2200 ~ 0x2208 | 系统信息 | 固件版本、序列号、运行时间、厂商信息 |

### 6.5 CANopen 通信参数

| 参数 | 默认值 | 可配范围 |
|------|--------|----------|
| Node ID | 1 | 1 ~ 127 |
| CAN 波特率 | 1 Mbps | 125k / 250k / 500k / 1M |
| Heartbeat 周期 | 200 ms | 50 ~ 5000 ms |
| SYNC 周期 | 1 ms | 1 ~ 50 ms |
| PDO 定时发送周期 | 10 ms（TPDO1/2） | 5 ~ 100 ms |
| EMCY 抑制时间 | 100 ms | 50 ~ 1000 ms |
| SDO 超时 | 500 ms | 100 ~ 2000 ms |

### 6.6 串口调试接口（UART）

UART 作为**纯调试通道**，提供交互式命令行终端，不参与正常业务控制。

#### 6.6.1 调试参数

| 参数 | 值 |
|------|-----|
| 波特率 | 115200 bps |
| 数据位 | 8 |
| 校验位 | None |
| 停止位 | 1 |
| 接口 | USB-TTL（3.3V） |

#### 6.6.2 CLI 命令集

采用类 Linux Shell 风格的命令行调试接口，命令格式：`<cmd> [arg1] [arg2] ...`

| 命令 | 参数 | 说明 | 示例 |
|------|------|------|------|
| `help` | — | 打印所有命令列表 | `help` |
| `status` | — | 打印当前全部状态信息（速度/位置/电流/温度/错误码） | `status` |
| `info` | — | 打印固件版本、序列号、运行时间 | `info` |
| `speed <rpm>` | 目标 RPM | 速度模式设定 | `speed 1500` |
| `pos <pulses> [abs\|rel]` | 脉冲数 + 模式 | 位置模式设定 | `pos 4000 abs` |
| `torque <ma>` | 目标电流(mA) | 力矩模式设定 | `torque 1500` |
| `stop [coast\|brake]` | 停止方式 | 正常停止 | `stop brake` |
| `estop` | — | 紧急停止 | `estop` |
| `reset` | — | 错误复位 | `reset` |
| `zero [manual\|auto]` | 归零方式 | 传感器归零 | `zero manual` |
| `canopen` | — | 打印 CANopen 网络状态、NMT 状态、COB-ID 表 | `canopen` |
| `param get <id>` | 参数 ID | 读取参数值 | `param get 0x1001` |
| `param set <id> <val>` | 参数 ID + 值 | 写入参数值 | `param set 0x2004 1.5` |
| `param save` | — | 参数保存到 Flash | `param save` |
| `param list` | — | 列出所有参数及当前值 | `param list` |
| `log <level>` | 日志等级 | 设置调试日志级别 (0=关闭/1=错误/2=警告/3=信息/4=调试) | `log 3` |
| `trace on\|off` | 开关 | 开启/关闭实时数据波形输出（速度/位置/电流曲线） | `trace on` |
| `reboot` | — | 软件复位 | `reboot` |

#### 6.6.3 调试日志格式

```
[<时间戳ms>] [<等级>] <模块>: <消息>
```

示例：
```
[001234] [INF] PID: Speed loop Kp updated to 1.50
[012500] [WRN] OC: Current approaching limit (5.2A / 6.0A)
[015000] [ERR] ESTOP: Emergency stop triggered (GPIO_PIN_8)
[017800] [DBG] CAN: RPDO1 received, target speed = 2000 rpm
```

Trace 模式下实时数据流输出（CSV 格式，配合 PC 上位机绘图）：
```
t,vel,pos,cur,tor,temp
0,-12,1024,0.50,0.08,35
10,150,2048,1.20,0.20,35
20,800,4096,2.10,0.35,36
...
```

---

## 7. 电机兼容性设计

### 7.1 可配置参数

系统通过**参数表**适配不同电机，参数存储在 Flash 中，上电加载：

| 参数ID | 参数名称 | 说明 | 单位 | 东政电机默认值 |
|--------|----------|------|------|----------------|
| 0x1001 | 编码器线数 | 编码器每圈脉冲数（4倍频前） | PPR | 1000 |
| 0x1002 | 额定电压 | 电机额定工作电压 | V | 24 |
| 0x1003 | 额定电流 | 电机额定持续电流 | A | 3.0 |
| 0x1004 | 峰值电流 | 短时允许最大电流 | A | 6.0 |
| 0x1005 | 额定转速 | 额定转速 | RPM | 3000 |
| 0x1006 | 额定扭矩 | 额定输出扭矩 | Nm | 0.5 |
| 0x1007 | 扭矩常数 | Kt = 扭矩/电流 | Nm/A | 0.167 |
| 0x1008 | 反电势常数 | Ke = 电压/转速 | V/kRPM | 8.3 |
| 0x1009 | 极对数 | 电机极对数 | — | 4 |
| 0x100A | 相电阻 | 电机绕组相电阻 | Ω | 0.8 |
| 0x100B | 相电感 | 电机绕组相电感 | mH | 1.2 |

### 7.2 PID 参数配置

| 参数ID | 参数名称 | 说明 | 东政电机默认值 |
|--------|----------|------|----------------|
| 0x2001 | CUR_Kp | 电流环比例系数 | 0.5 |
| 0x2002 | CUR_Ki | 电流环积分系数 | 0.02 |
| 0x2003 | CUR_Kd | 电流环微分系数 | 0 |
| 0x2004 | SPD_Kp | 速度环比例系数 | 1.2 |
| 0x2005 | SPD_Ki | 速度环积分系数 | 0.08 |
| 0x2006 | SPD_Kd | 速度环微分系数 | 0.01 |
| 0x2007 | POS_Kp | 位置环比例系数 | 8.0 |
| 0x2008 | POS_Ki | 位置环积分系数 | 0 |
| 0x2009 | POS_Kd | 位置环微分系数 | 0.5 |

### 7.3 保护阈值参数

| 参数ID | 参数名称 | 说明 | 默认值 |
|--------|----------|------|--------|
| 0x3001 | OV_THOLD | 过压阈值 | 28.8V (24V×1.2) |
| 0x3002 | UV_THOLD | 欠压阈值 | 16.8V (24V×0.7) |
| 0x3003 | OC_THOLD | 过流阈值 | 6.0A (峰值电流) |
| 0x3004 | OL_THOLD | 过载扭矩阈值 | 0.75Nm (额定×1.5) |
| 0x3005 | OL_TIME | 过载允许时间 | 3.0s |
| 0x3006 | STALL_CUR | 堵转判定电流 | 2.0A |
| 0x3007 | STALL_TIME | 堵转判定时间 | 0.5s |
| 0x3008 | OT_DRV | 驱动器过热阈值 | 85℃ |
| 0x3009 | OT_MOT | 电机过热阈值 | 120℃ |
| 0x300A | COMM_TOUT | 通信超时时间 | 500ms |

---

## 8. 运行状态机

```
                    ┌─────────────────────────────┐
                    │        上电初始化             │
                    └─────────────┬───────────────┘
                                  │
                    ┌─────────────▼───────────────┐
                    │        参数加载               │
                    └─────────────┬───────────────┘
                                  │
                    ┌─────────────▼───────────────┐
              ┌─────│       未使能 (Disable)       │◄──── 复位完成 ────┐
              │     └─────────────┬───────────────┘                    │
              │                   │ 使能指令                           │
              │     ┌─────────────▼───────────────┐                    │
              │     │       就绪 (Ready)           │                    │
              │     └─────────────┬───────────────┘                    │
              │                   │ 控制指令                           │
              │     ┌─────────────▼───────────────┐                    │
              │     │       运行 (Running)         │                    │
              │     │  ┌──────┐ ┌──────┐ ┌──────┐ │                    │
              │     │  │速度模│ │位置模│ │力矩模│ │                    │
              │     │  │  式  │ │  式  │ │  式  │ │                    │
              │     │  └──────┘ └──────┘ └──────┘ │                    │
              │     └─────────────┬───────────────┘                    │
              │                   │                                    │
              │     ┌─────────────▼───────────────┐                    │
              └─────│       故障 (Fault)           │─── 手动复位 ──────┘
                    └─────────────────────────────┘
```

---

## 9. 非功能需求

### 9.1 实时性

| 指标 | 要求 |
|------|------|
| 电流环控制周期 | ≤ 50μs |
| 速度环控制周期 | ≤ 1ms |
| 位置环控制周期 | ≤ 5ms |
| 急停响应时间 | ≤ 5ms |
| 通信响应时间 | ≤ 10ms（PDO）/ ≤ 50ms（SDO） |

### 9.2 可靠性

- MTBF ≥ 10000 小时（电气部分）
- 看门狗定时器：独立窗口看门狗（WWDG），1.5ms 喂狗周期
- 上电到就绪时间 ≤ 500ms
- Flash 参数写入寿命：读取次数不限，写入 ≥ 10000 次（带磨损均衡）

### 9.3 环境适应性

| 指标 | 要求 |
|------|------|
| 工作温度 | -20℃ ~ +70℃（驱动器） |
| 存储温度 | -40℃ ~ +85℃ |
| 工作湿度 | 10% ~ 90%RH（无凝露） |
| 防护等级 | 驱动器 PCB 涂覆三防漆 |

---

## 10. 测试验证需求

### 10.1 东政电机测试项

| 测试编号 | 测试项 | 方法 | 判定标准 |
|----------|--------|------|----------|
| T-001 | 编码器读取验证 | 手动转动电机，读取编码器计数值 | 正向递增、反向递减、无跳变 |
| T-002 | 速度阶跃响应 | 0→2000RPM 阶跃指令 | 超调≤5%，调节时间≤200ms |
| T-003 | 位置定位精度 | 往返运动 10 次 | 返回零点误差≤±2脉冲 |
| T-004 | 力矩控制精度 | 悬吊砝码，读取实际扭矩 | 误差≤±5% |
| T-005 | 急停功能 | 运行中触发急停 | PWM停止输出 ≤ 5ms |
| T-006 | 过流保护 | 人为堵转电机 | 1s内触发保护停机 |
| T-007 | 过热保护 | 热风枪加热温度传感器 | 85℃触发降功率/停机 |
| T-008 | 参数掉电保存 | 修改参数后断电重启 | 参数完整恢复 |
| T-009 | 长时间运行 | 额定负载连续运行 2h | 无故障、温度稳定 |
| T-010 | CANopen PDO 通信 | 运行中连续收/发 PDO 帧 10000 次 | 无误码、无帧丢失 |
| T-011 | CANopen EMCY 报警 | 人工触发过流/过热故障 | EMCY 报文在 5ms 内上送，错误码正确 |
| T-012 | CANopen NMT 状态切换 | 上位机依次发送 Start/Stop/Pre-Op/Reset | NMT 状态切换正确，Heartbeat 反馈状态正确 |
| T-013 | SDO 参数读写 | 通过 SDO 读写全部配置参数 | 数据校验通过、掉电保存恢复正常 |
| T-014 | UART CLI 功能 | 通过串口终端输入各类调试命令 | 所有命令正常响应，日志输出正确 |
| T-015 | 通信可靠性 | 连续收发 10000 帧 | 无误码 |

---

## 11. 附录

### 11.1 术语表

| 术语 | 说明 |
|------|------|
| PPR | Pulse Per Revolution，编码器每转脉冲数 |
| M/T法 | 综合测速法，结合M法和T法的优点 |
| Kt | 扭矩常数，Nm/A |
| Ke | 反电势常数，V/kRPM |
| 三环控制 | 电流环→速度环→位置环的串级控制结构 |

### 11.2 参考文献

- STM32F4 参考手册 — 定时器/编码器接口章节
- CiA 301 — CANopen Application Layer and Communication Profile (V4.2.0)
- CiA 402 — CANopen Drive and Motion Control Device Profile (V4.0.0)
- 《直流电机调速系统设计》(PID控制理论)
- 东政电机产品手册 — 编码器电机规格参数
